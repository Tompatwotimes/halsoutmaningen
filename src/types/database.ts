/**
 * Placeholder for Supabase-generated types.
 *
 * Once the schema migrations exist, regenerate with:
 *   npm run db:types
 *
 * Until then this keeps the typed client compiling without asserting a schema
 * shape we have not built yet.
 */
export type Database = Record<string, never>;
